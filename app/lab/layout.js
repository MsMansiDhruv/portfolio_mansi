export const metadata = {
  title: "Particle Portrait Lab",
  robots: { index: false, follow: false },
};

/** Isolated lab - no site chrome. */
export default function LabLayout({ children }) {
  return children;
}
