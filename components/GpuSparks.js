"use client";

/**
 * Legacy cursor sparkles — disabled site-wide. Kept so old imports stay harmless.
 * Canvas is removed on unmount via AppShell route cleanup if it was left behind.
 */
export default function GpuSparks() {
  return null;
}
